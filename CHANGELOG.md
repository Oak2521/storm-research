# Changelog

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-06-24

围绕"agentic 检索"与"输出结构"的实质升级，并加固跨平台执行的稳定性。

### 新增

- **Step 3 agentic 检索闭环（反思与补搜）**：把检索从一次性动作，升级为「搜 → 判断够不够 → 不够则诊断 → 调整再搜」的迭代闭环。核心纪律「不够不是布尔值，是一份诊断」——诊断五类（跑题/太浅/冲突/信源弱/新缺口）对应五种调整，新 query 必须由缺口驱动而非重复原 query。每子问题最多 3 轮、每轮干不同的事，首轮充分即提前退出，到顶仍不够则诚实标注缺口、禁止脑补。原理见 `reference/agentic-retrieval.md`（含 Self-RAG / CRAG / FLARE / IRCoT / Adaptive-RAG 等一手来源）。
- **三张概念图**：核心机制、平均化共识 vs 真实分歧、跨平台 + 自动复用工具（见 README）。

### 改进

- **输出物重新设计为信息量优先的固定骨架**：一、基础与背景 → 二、核心机制/全景 → 三、多视角深入 → 四、矛盾分析。先把定义与来龙去脉讲透（信息密度），再展开多视角讨论，矛盾分析降为支撑章节。第三层每节用「问题钩子 + 带源回答」展开。
- **流程整合与稳定化**：步骤重编号为 Step 0–7，把原 3/3.5/4 合并为一个原子的 Step 3「检索·反思·作答」。新增「完成的定义」——唯一交付物是带 `[n]` 引用 + 参考来源列表的完整报告，杜绝停在中间把过程小结当答案。
- **Step 0 强制证据门槛**：必须先实际运行遍历命令并贴出原始输出，再据此列检索清单；杜绝凭记忆只报 WebSearch/WebFetch 而漏掉 `mywebsearch`、`perplexity-search`、`scrape`、`content-researcher` 等真实可用工具。附开箱即用的跨平台扫描命令。

[1.1.0]: https://github.com/openwhat007/storm-research/releases/tag/v1.1.0

## [1.0.0] - 2026-06-21

首个正式版本。跨平台 AI 深度研究 skill：多视角提问 + 真实联网检索 + 强制引用。

### 核心能力

- **Step 0 检索能力探测**：研究开始前自动遍历当前环境真实可用的搜索工具、搜索/调研类 skill、检索子 agent，列成清单全程复用。靠"实际遍历 + 只读描述字段"发现，避免凭记忆漏掉领域检索工具与调研 agent。
- **跨平台兼容**：兼容 Claude Code / Codex / OpenClaw / Hermes / Cursor / Cline / Gemini / OpenCode 等，路径"方法不变、只换目录"自动适配。
- **多视角提问**：围绕主题生成互相对立的研究者视角，冲突暴露单一框架的盲区。
- **双引擎广度**：对立视角负责深层 + 跨框架覆盖，覆盖视角做系统性话题分解（query decomposition）保证基础话题不漏。
- **检索接地 + 强制引用**：每个论断基于真实检索，正文内联 `[n]`、文末列真实 URL，检索不到则明确拒答。
- **检索韧性兜底**：工具失效时逐级降级（换工具 / 抓权威域名 / 派检索子 agent）。
- **矛盾图与缺口发现**：跨视角提炼分歧、共识、盲区。
- **过程可见性契约**：Step 0-5 每步先输出 checkpoint，避免多 agent 研究退化为黑箱。
- **自我同行评审**：报告产出后自查强/弱论点、偏见、缺失角度、来源可靠性。

### 致谢

方法思想源自 Stanford OVAL 的 STORM（NAACL 2024, arXiv:2402.14207），在其基础上做了面向真实 agent 环境的工程化扩展。详见 README「来源与引用」。

[1.0.0]: https://github.com/openwhat007/storm-research/releases/tag/v1.0.0
