# 贡献指南

欢迎为 storm-research 贡献。这是一个跨平台的 agent skill，核心是**方法**而非代码，所以贡献也更偏向"打磨提示词与流程"，而不是写程序。

## 仓库结构

```
storm-research/
├── SKILL.md                 # 主入口：Step 0 + 8 步流程骨架、三根支柱、可见性契约
├── prompts/                 # 每一步的提示词内核
│   ├── 00_findsearchtools.md   # Step 0 检索能力探测（跨平台）
│   ├── 01_personas.md          # 视角发现（对立视角 + 覆盖视角）
│   ├── 02_question.md          # 带视角提问 / 话题分解
│   ├── 03_query.md             # 问题转搜索词 → 真实检索
│   ├── 04_answer.md            # 检索作答（抗幻觉）
│   ├── 05_contradiction.md     # 矛盾图 / 缺口发现
│   ├── 06_outline.md           # 两步法大纲 + 话题完整性核对
│   ├── 07_write.md             # 带引用写作
│   └── 08_review.md            # 自我同行评审
└── reference/
    └── methodology.md       # 方法论原理（与实现无关）
```

## 改动原则

1. **改 `prompts/` 时，同步更新 `SKILL.md` 里对应的摘要**。两者是"详述 ↔ 摘要"的关系，不能脱节。
2. **每条流程纪律最好有实测依据**。本项目的多数规则来自实跑暴露的失败模式（如"凭记忆列举会漏掉检索工具""全文 grep 噪声爆炸"）。提 PR 时请说明改动解决的是哪个具体问题。
3. **保持跨平台**。涉及路径/命令的改动，不要写死单一平台；遵循 `00_findsearchtools.md` 的"方法不变、只换目录"原则。
4. **守住三根支柱**：多视角、检索接地、强制引用。任何让流程能"跳过检索"的改动都违背项目初衷。
5. **语言**：提示词与文档以中文为主，术语可中英并用。

## 提 PR 流程

1. Fork 本仓库，从 `main` 切出特性分支（如 `feat/xxx`、`fix/xxx`）。
2. 做改动，并在本地用一个真实主题跑一遍受影响的步骤，验证效果。
3. PR 描述里写清楚：**解决什么问题 / 改了哪些文件 / 如何验证**。
4. 涉及流程行为变化的，请在 PR 里贴一小段实跑前后的对比。

## 版本与发布

- 采用语义化版本（SemVer）。流程行为变化记 minor，提示词措辞微调记 patch。
- 变更记录维护在 `CHANGELOG.md`（如有）。

## 致谢

方法思想源自斯坦福 OVAL 的 STORM 研究，详见 README 的「来源与引用」。
