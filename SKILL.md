---
name: storm-research
description: 做带来源、可追溯的深度研究。多视角提问 + 真实联网检索 + 强制引用，产出维基百科式的完整研究报告。当用户说"深度调研""帮我研究一下 X 这个主题""写一篇带来源的综述""多角度研究"时触发。
---

# 多视角检索式深度研究

研究的瓶颈不是"写"，而是"问"。把主题直接交给模型，它只会给你主流框架下的平均答案。这套方法让模型像博士生做预调研那样工作：**从多个对立视角追问 + 每个回答都用真实检索接地 + 每句话可追溯到来源**。

方法论原理见 `reference/methodology.md`；agentic 反思补搜原理见 `reference/agentic-retrieval.md`。

## 三根支柱（缺一根就退化）

1. **多视角（Multi-perspective）** — 不同视角制造问题的广度与深度，视角间的冲突暴露单一框架看不到的东西
2. **检索接地（Retrieval）** — 每个回答必须基于检索工具取回的真实内容，无来源就明确拒答，绝不编造
3. **强制引用（Citation）** — 正文每个论断内联 `[n]`，文末列真实 URL

**红线：跳过检索，就不再是研究，而是让一个模型扮演几个角色自说自话——它们共享同一套盲区，会满怀自信地一起幻觉。宁可慢，不可省。**

## ✅ 完成的定义（Definition of Done —— 先读这条）

**这套流程只有一个交付物：一篇带 `[n]` 内联引用 + 参考来源 URL 列表的完整 markdown 研究报告。** 走完 Step 7 写出这篇报告，才算完成。

- ❌ **不允许**：停在中间步骤（视角、问题、矛盾图）就把过程小结当成最终答案交付。那些是 checkpoint，不是产物。
- ❌ **不允许**：交一份没有 `[n]` 引用、没有参考来源列表的"聊天式总结"。
- ✅ **必须**：八步全部走完，最后输出「产出物」章节定义的完整报告结构。
- ✅ 报告应**写入文件**（如 `output/<主题>.md`），不只是贴在对话里——便于跨平台复用与归档。

> 一句话：**checkpoint 让过程透明，报告才是结论。没走到带引用的报告，就是没做完。**

## 执行流程总览

一条信息精炼流水线，**顺序执行、不可跳步、必须走到终点**：

```
Step 0  检索能力探测（一次性）
─ 阶段一 · 知识采集 ─────────────
Step 1  视角发现
Step 2  带视角提问
Step 3  检索 · 反思 · 作答（每个子问题的 agentic 闭环）
─ 阶段二 · 综合与组织 ───────────
Step 4  矛盾图 / 缺口发现
Step 5  两步法大纲
─ 阶段三 · 写作与交付 ───────────
Step 6  带引用写作 ← 交付物在这里产生
Step 7  自我同行评审
```

每步的提示词内核在 `prompts/` 下。

### 可见性契约（让过程透明，但别用它代替报告）

这套方法的价值一半在结论、一半在**过程可被检查**。所以**每完成一个知识采集步骤（Step 0-4），先甩一块紧凑 checkpoint 给用户，再进入下一步**——但 checkpoint 是进度信号，**不是交付物**，打完 checkpoint 必须继续往下走，直到 Step 6/7 产出报告。

- **checkpoint 要短、可扫读、固定格式**，目标是"让用户 5 秒看懂这步产出了什么"，不是把中间思考全倒出来。
- **不准把视角/提问藏进子 agent 的 prompt 里就算数**。Step 1、2 的产物必须先显式输出给用户，再据此去检索。
- **派发子 agent 检索时，先告诉用户每个 agent 负责哪个视角/哪批问题**，别让并行检索变黑箱。
- 写作阶段（Step 5-7）直接体现在最终报告里（大纲→正文→自查），不必单独打 checkpoint。

| 步骤 | 必须可见的 checkpoint |
|------|----------------------|
| Step 0 | 探测到的检索能力清单（工具/skill/agent，及优先级） |
| Step 1 | 视角列表：`视角名 — 一句关注点`，含覆盖视角 |
| Step 2 | 每个视角下的问题清单（对立视角 2-3 个深问题；覆盖视角的话题分解清单） |
| Step 3 | ①检索分工：哪个 agent/工具查哪批问题；②引用表 `[n]→{url,title,tool}`；③反思补搜结果：哪些首轮即过、哪些补搜几轮（诊断→调整）、哪些标记缺口 |
| Step 4 | 矛盾图：分歧点、各方立场、一致项、盲区 |

> 一句话纪律：**用户应该在最终报告之前就看到视角、问题、检索分工、矛盾图；并且最终一定要看到那篇带引用的报告。**

---

## Step 0 — 检索能力探测（研究开始前，一次性） （见 `prompts/00_findsearchtools.md`）

正式研究前做一次性的环境探测，**发现并列出当前可用的所有检索能力**，全程复用。与主题无关，只做一次。

> ⚠️ **核心纪律：靠回忆必然遗漏。必须实际遍历，逐项过筛——不是凭印象按类联想。**

> 🚧 **强制证据门槛**：最高频的失败是 agent 跳过遍历、**凭记忆只报 WebSearch/WebFetch 两三个工具**就算完。所以：**必须先实际跑遍历命令（见 `prompts/00_findsearchtools.md`），把原始输出贴给用户，再据此列清单。没贴原始输出 = Step 0 没做。** 自检反问：我是不是只写了 WebSearch/WebFetch？是的话几乎肯定漏了——常用搜索类（google、brave、tavily、bing、perplexity、web-search 等）往往以 skill/MCP 形式并存；遇到拿不准的工具名就快速搜一下它是不是搜索/抓取/调研工具。回去跑命令。

- **发现三类检索资产，优先级 搜索＞调研**（storm 本质是 research，搜索类是核心、调研类辅助）：①搜索/检索**工具** ②搜索/调研类 **skill** ③搜索/调研类**子 agent**。
- **看描述、不看目录名**：skill 读 `name`+`description`、agent 读 `description`+`tools`，**只在描述字段里筛**搜索/调研关键词（全文 grep 噪声爆炸），机器粗筛后逐个精判。子 agent 判定：`tools` 含 WebSearch/WebFetch 或等价物、或职责提"调研/research/搜索"即纳入（如 content-researcher / research-analyst / general-purpose）——内置工具撞墙时这是最强检索路径。
- **跨平台**：兼容 Claude Code / Codex / OpenClaw / Hermes / Cursor / Cline / Gemini / OpenCode 等。各平台 skill/agent 位置不同，**方法不变、只换目录**（详见 00 的路径表）；只纳入当前 runtime 真能调用的，无手段则标注"未完整遍历"。
- **宽口径过筛**：通用搜索 / 抓取 / 领域检索（法律·学术·文档…）/ 调研类 skill / 检索子 agent / 命令行间接通道（curl·gh）/ 需授权工具。
- **排除本地/私域**：只检索本地记忆、本地代码、私人网盘/聊天的工具不算接地能力（接地只认"能取回外部公开来源"）。
- **按主题排优先级**：对口的领域检索工具优先，通用搜索退为补充。
- **早失败**：遍历后确认无任何检索通道，立刻告知用户"无法做接地研究"，不浪费后续步骤。

---

## 阶段一 · 知识采集（方法的灵魂）

### Step 1 — 视角发现 （见 `prompts/01_personas.md`）
- 围绕主题生成 3-5 个**差异化视角的研究者 persona**（如：一线实践者 / 怀疑论者 / 追踪资金的经济视角 / 见过历史模式的历史视角 / 读过研究的学术视角）。
- 再加一个**覆盖视角（默认必加）**：不带立场，负责系统性话题分解（query decomposition），穷举该类主题的标准话题面，保证基础完整性。
- 每个 persona 写一句关注点说明。
- **两种广度并行**：对立视角管"深层 + 跨框架"覆盖，覆盖视角管"机械完整性"。
- ⚠️ **先把视角列表输出给用户，再进 Step 2**。不准只存在脑子里或直接塞进 agent prompt。

### Step 2 — 带视角提问 （见 `prompts/02_question.md`）
- 对立视角：每个生成 2-3 个**深问题**，一次一个、追问往深处走。
- 覆盖视角：走**话题分解模式**——把标准话题面（定义/机制/证据/安全/用法/同类对比/监管…）穷举成可检索问题，和对立视角的问题一起进入检索流水线。
- 这些问题决定研究的广度和深度，是整个系统最关键的一步。
- ⚠️ **先把"每个视角 → 它的问题清单"输出给用户，再进 Step 3 检索**。这是整套方法的灵魂，藏起来等于没做。

### Step 3 — 检索 · 反思 · 作答（每个子问题的 agentic 闭环） （见 `prompts/03_query.md`、`prompts/03b_reflect.md`、`prompts/04_answer.md`）

这是 naive RAG 与 agentic search 的分界线：检索不是一次性动作，而是**对每个子问题跑「搜→判断够不够→不够就诊断调整再搜→够了带证据作答」的闭环**。原理见 `reference/agentic-retrieval.md`。

**3a. 问题转搜索词 + 真实检索**
- 每个问题转成 1-3 条搜索词，用 **Step 0 探测到的检索能力清单**去检索（不在这里重新发现工具）。
- 多个检索能力时**尽可能多地并行调用**，多源交叉验证。
- **某个搜索工具返回空/失效时不要停**：换下一个工具，或对权威域名（arXiv、PubMed、官方站、维基）用抓取工具取正文兜底。
- 降级阶梯：内置检索工具 → 检索类 MCP/skill → 派发检索子 agent → 浏览器自动化兜底 → 都没有才据实告知无法接地。
- 给每条来源编号，建立并全程维护引用表：`[n] → {url, title, snippet, tool}`。
- ⚠️ **派发子 agent 前先告诉用户检索分工**；检索回来后亮出引用表雏形。

**3b. 反思与补搜（agentic 核心）**
- **核心纪律：「不够」不是布尔值，是一份诊断**。判定不够却把同一个 query 再搜一遍 = 空转烧 token（over-searching，和漏搜一样是失败）。反思必须输出「差在哪 + 做什么调整」。
- **诊断五类 → 对应调整**：①跑题→改写 query；②太浅→下钻；③冲突→定向取证；④信源弱→换权威源；⑤新缺口→派生子问题。
- **调整四杠杆**：调 query（铁律：新 query 由"缺口"驱动，**不能重复原 query**）／调信源工具／调粒度／派生新子问题。
- **每子问题最多 3 轮，每轮干不同的事**；**提前退出**：首轮就充分且一致的子问题直接通过，别让基础话题吃掉预算。

**3c. 检索作答（抗幻觉）**
- 基于取回的信息作答，**每句话都要有来源支撑**，标注对应 `[n]`。
- 信息不足时明确写"基于现有信息无法回答"并说明缺口。**禁止用模型记忆补全**。

---

## 阶段二 · 综合与组织

### Step 4 — 矛盾图 / 缺口发现 （见 `prompts/05_contradiction.md`）
- 跨视角找出专家分歧点：各视角在哪里打架，为什么。
- 全部一致的大概率为真；没人提到的就是整个领域的盲区/机会。
- 这一步把"表面理解"升级为"真正的理解"，多数人会跳过它。

### Step 5 — 两步法大纲（固定骨架：信息量优先） （见 `prompts/06_outline.md`）
- **报告骨架是固定的递进结构，顺序不可颠倒**：一、基础与背景 → 二、核心机制/全景 → 三、多视角深入 → 四、矛盾分析。
- **信息量在前、讨论在后**：一二层先把定义、概念、来龙去脉、整体机制讲透（信息密度）；三层才展开多视角讨论；矛盾分析降为支撑章节，不是高潮。
- **第三层用「问题钩子」**：每个子节用一个真问题（取自 Step 2 视角问题）开场，再给带源回答。
- 先凭固定骨架起草，再用采集信息精修；做话题完整性核对，缺席的基础话题回 Step 3 补检索，禁止脑补。

---

## 阶段三 · 写作与交付（不可跳过——交付物在这里产生）

### Step 6 — 带引用写作 （见 `prompts/07_write.md`）
- 按骨架层**逐层写**：基础 → 机制 → 多视角（问题钩子）→ 矛盾分析。
- 一二层信息密度优先、多用带源陈述；三层每个 `###` 是「问题钩子 + 带源回答」。
- **内联 `[n]` 引用，n 对应 Step 3 引用表里的真实 URL**。这是硬性要求，没有引用的报告不合格。
- 开头写 `# 摘要` 引导段（≤4 段，可用核心问题开场做钩子）。
- 文末输出 `## 参考来源`，把引用表里所有 `[n]` 的真实 URL 一一列出。

### Step 7 — 自我同行评审 （见 `prompts/08_review.md`）
- 给自己的研究打分：强论点 / 弱论点 / 潜在偏见 / 缺失角度 / 各来源可靠性。
- 去除重复内容，保留引用和结构。

---

## 产出物（交付物的唯一标准）

**一篇 markdown 完整研究报告**（建议写入 `output/<主题>.md`），遵循**固定骨架 + 信息量优先**的递进结构：

1. `# 摘要` —— 全文概述，可用核心问题开场做钩子（≤4 段）
2. **一、基础与背景** —— 定义、关键概念、来龙去脉。信息密度最高的一层，重事实重引用
3. **二、核心机制 / 全景** —— 运作方式、组成/谱系、关键维度、主流方案横向对比
4. **三、多视角深入** —— 每个子节是「**问题钩子 + 带源回答**」，展开丰富讨论
5. **四、矛盾分析** —— 分歧点、共识项、领域盲区（支撑章节，简明，非高潮）
6. `## 参考来源` —— 编号对应的**真实 URL 列表**（缺这条 = 不合格）
7. `## 自查报告` —— 强/弱论点、偏见、缺失角度、可靠性评分

> **结构纪律**：先信息量（一、二层把"是什么、怎么运作"讲透）、后讨论（三层多视角）、矛盾分析降为支撑。不许跳过基础层直接进多视角，也不许把矛盾分析提前当主菜。
>
> 自检：报告有 `[n]` 吗？有参考来源 URL 列表吗？一二层信息量够吗？三层是问题钩子形态吗？七部分齐吗？缺任意一项，回到对应 Step 补齐，不要交付半成品。

## 重要原则

- **必须走到报告**：checkpoint 不是答案，停在中间 = 没完成。
- **检索不可省**：每个视角的回答都要有真实来源。这是 research 与 persona-prompting 的分界线。
- **诚实标注边界**：检索不到就说检索不到。报告里区分"有来源支撑"和"推断"。
- **主流之外留心眼**：这套方法本质是综合网上主流说法，对冷门/非共识话题容易流于平庸。遇到这类主题，在自查报告里明确提示"本报告偏向主流框架，可能遗漏非常规视角"。
- **方法论原理**见 `reference/methodology.md`、`reference/agentic-retrieval.md`。
